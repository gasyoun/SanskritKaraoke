# Dependencies for the Sanskrit Karaoke teaching pipeline (agents/teaching_pipeline/)
#
# Install:  pip install -r requirements.txt
# Dev:      pip install -r requirements.txt pytest pytest-asyncio
#
# PostgreSQL persistence (production):
#   pip install psycopg[binary]   ← psycopg v3, async-compatible (preferred)
#   psycopg2-binary               ← v2, sync-only fallback (already listed below)

# LangGraph + LangChain
langgraph>=1.2.2
langchain>=0.2.0
langchain-anthropic>=1.4.6
langchain-google-genai>=4.2.4

# LLM Providers
anthropic>=0.25.0
google-generativeai>=0.5.0
requests>=2.31.0

# Data validation
pydantic>=2.13.4

# Verse schema validation
jsonschema>=4.26.0

# Database drivers
psycopg[binary]>=3.1.0   # PostgreSQL async (preferred for production)
psycopg2-binary>=2.9.0   # PostgreSQL sync fallback
# sqlite3 is part of the Python standard library and must not be installed via pip.

# Environment
python-dotenv>=1.2.2
