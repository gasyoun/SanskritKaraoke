- **Retired Claude model IDs replaced in the two `agents/` API call sites (24-09-2026, Claude Code Opus 5 `claude-opus-5`, H5352).** Claude Sonnet 3.5 was retired on 28-10-2025 and both call sites 404'd. [agents/teaching_pipeline/llm.py](https://github.com/gasyoun/SanskritKaraoke/blob/main/agents/teaching_pipeline/llm.py) now calls `claude-sonnet-5` with `max_tokens=16000` (thinking is adaptive-by-default on Sonnet 5 and counts toward `max_tokens`, so the old 1024 cap would have been eaten before any answer), and its cost table carries the Sonnet 5 rate `(2.00, 10.00)`. [agents/verse_agent_raw.py](https://github.com/gasyoun/SanskritKaraoke/blob/main/agents/verse_agent_raw.py) `detect_meter()` now calls `claude-sonnet-5` with `max_tokens=1024` and `output_config={"effort": "low"}` — it is a one-word classification, and on failure the old code returned `"error: …"` as a meter name. Mock cost fixture [agents/teaching_pipeline/create_mock_logs.py](https://github.com/gasyoun/SanskritKaraoke/blob/main/agents/teaching_pipeline/create_mock_logs.py) re-priced to match. [docs/history/verse_agent_raw.py](https://github.com/gasyoun/SanskritKaraoke/blob/main/docs/history/verse_agent_raw.py) is a history copy nothing imports and keeps the old ID deliberately. Source: [claude-config PROMPT_AUDIT_INSTRUCTION_ESTATE_AND_API_CODE_23-09-2026.md](https://github.com/gasyoun/claude-config/blob/main/docs/PROMPT_AUDIT_INSTRUCTION_ESTATE_AND_API_CODE_23-09-2026.md) hunks F1–F2. No live smoke call — no `ANTHROPIC_API_KEY` on the executing box, so the new IDs are unverified against the live API.
