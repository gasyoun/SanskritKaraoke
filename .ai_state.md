# Project Objective: Sanskrit Karaoke v1.4.2 first release

## ➡️ Next Steps (Queue)

### Security (remaining)
- (none — SRI done)

### Data / schema
- [ ] Resolve translation provenance: EN translations of bhg_2_47/2_49 look like Prabhupāda (BBT copyright) — replace with own translations or confirm rights; set provenance to `human`/`human-reviewed`.

### Eval harness activation
- [ ] Get ≥3 audio files + session exports → `extract_timing_from_session.py` → `eval_alignment.py --audio <dir>` → tune `tools/alignment_params.json` if needed (acceptance target ≥90% within ±50ms).

### Render pipeline
- [ ] `npm install --prefix tools && node tools/render_chapter.js <audio_dir>` — first real end-to-end headless render test.

### ADR-0001 remaining extraction (separate PRs)
- [ ] `_renderPngCanvas` → `compose.js` (6 callers; app.js PNG export vs render.html composePngFrame — drift risk)
- [ ] Karaoke camera → `karaoke-frame.js` (downloadKaraokeMp4 has inline camera math; render.html already uses karaoke-frame.js updateCamera/drawStoryFrame — drift risk)
- [ ] `corpusScaleTiming` data-accessor injection → make the verses/ fetch injectable so Node can call it (scale math already extracted)

### Architecture (medium priority, no blocker)
- [ ] `student.html` hidden-bridge removal — blocked until all remaining ADR-0001 DOM deps are severed (opt-wave-scale, opt-dots, getComputedStyle colors)
- [ ] Cloud sync auth state machine — three pages (index, student, progress) each wire Firebase auth differently; consolidate into a shared `auth-state.js`

### Firebase / Drive acceptance
- [ ] Run cloud-sync acceptance test: student SRS rating → Firestore → second device → `progress.html`.
- [ ] Verify Google login/logout against deployed origin (blocked at password/2FA on last attempt).
- [ ] Add real Google Drive audio file IDs to the 3 example verses.

### Legal / licensing
- [ ] Fill open blanks in `docs/legal/Usha_Sanka_License_Agreement_DRAFT.docx`: addresses, governing law (§7.1), consideration alternative (§3.1), Annex A dates/durations.
- [ ] Phase 3: post-kit generator with UTM-tagged course CTAs (free-video → paid-course funnel).

---

## 🚧 Current Work-In-Progress (WIP)

**Paused — PR #9 awaiting review/merge.** Branch `review/core-extraction-and-hardening-jun-2026`
(11 commits: ADR-0001/0002/0003 + multi-review hardening + SRI + APTE lazy-load + pada wire-up).
origin/main was 8 behind at branch time, so the PR spans all of it.

Next thread (deferred to a **fresh PR** after #9 merges): the two ADR-0001 drift
consolidations — `_renderPngCanvas` → `compose.js` and karaoke camera →
`karaoke-frame.js` (each is a separate impl in app.js vs render.html → real drift risk).

---

## 🧠 Dev Notes & Hypotheses

### Architecture findings (2026-06-13)
- `document.addEventListener('click', closeCtx)` was inside `render()` — accumulated a listener per keystroke. Fixed: moved to module-init (once).
- `_mainHighlightStart()` RAF guard (`if (!_mainHlRaf)`) was already correct — review agent flagged a false positive.
- `openTimingEditor` / `closeTimingEditor` listener pairing is symmetric and correct — no leak. The timing-editor concern in the review was a false positive.
- `_getWaveColors()` is now a named function; headless renderers can shadow it without touching `buildWaveSVG()`.
- `runPipeline()` now has a try/catch; uncaught meter-detection / syllabification throws surface as `showMsg` instead of locking UI.

### Data/schema findings (2026-06-13)
- `TAP.confidence` is populated **only** by the whisper-v1 aligner (`teAutoTimingAndSwitch`), never by manual timing edits. Documented in CLAUDE.md.
- `arrow` field on DATA syllables is serialized in session JSON but absent in verse JSON files. Now documented in CLAUDE.md.
- `vipulaType` known variants: `ma-vipulā`, `bha-vipulā`, `na-vipulā`, `ra-vipulā`. Documented.
- `duration_s: 15.5` was a template sentinel in all 3 sample verses; replaced with `null` and schema updated to `["number","null"]`.

### Security findings (2026-06-13)
- Drive `_gdRenderList()` XSS: `f.name` was embedded in onclick= HTML attribute with only `'` escaping — backslash injection bypassed it. Fixed with `createElement` + `addEventListener`.
- `showMsg()` was missing `>` and `"` from its escape chain — potential XSS if caller ever passes external content.
- Firebase/Google API keys in client code are public-by-design — security is via Firestore rules + authorized domains, NOT key secrecy.

### Legacy notes (from prior sessions)
- 2026-06-03: Firebase redirect auth is wired; blocked at password/2FA/consent on last live test.
- 2026-06-03: Firestore rules committed for `users/{uid}/data/{srs_v1,telemetry,progress_meta}`; need deployment in Firebase Console.
- 2026-06-02: SW cache now uses `cache: reload` for student URLs; version query strings prevent stale HTML after fixes.
- 2026-06-02: Telegram BackButton calls guarded by SDK version support checks.

---

## ✅ Completed (Recent only)

### 2026-06-13 — Pada-detection: wire up + extract (ADR-0001)

- [x] **Found dead code:** `detectPadaBounds` (audio-based auto pada detection) + its helper `_showRmsData` + `copyPadaBounds` were all unreachable — the live "Указать пады" button (`teMarkPadas`) used plain uniform division. They were remnants of a superseded main-window pada UI.
- [x] **Extracted** the RMS-search core to [`core/timing.js`](src/core/timing.js) `detectPadaBoundsFromPcm` + `PADA_DEFAULTS` (mirrors `alignment_params.json`).
- [x] **Wired it up:** `teMarkPadas` now auto-detects the 4 padas from the 3 longest audio pauses when PCM is loaded, with uniform-division fallback + a success message.
- [x] **Deleted** the 3 dead functions (181 lines): app.js 333,563 → 325,677 bytes.
- [x] Verified: `detectPadaBoundsFromPcm` **byte-identical** to the old inline algo on synthetic PCM (4 padas at the injected pauses); empty-audio and <3-pause cases handled; module loads, `teMarkPadas`/`runPipeline` on window, zero console errors; `node --check` clean.

### 2026-06-13 — APTE_METERS lazy-load (ADR-0001)

- [x] Extracted `APTE_METERS` (90 entries) + `APTE_METERS2` (106 entries) inline blobs out of app.js into [`src/data/apte_meters.json`](src/data/apte_meters.json) (~203 KB). Estimate was "~55 KB" — actually **204 KB** combined; app.js dropped 536,611 → 333,563 bytes (**−38%**).
- [x] Converted via Node eval of the literal (not a JS→JSON regex) for byte-fidelity. Lazy `loadApteMeters()` fetches once, caches via `_apteMetersPromise`; mutable `APTE_METERS`/`APTE_METERS2` globals keep all sync consumers unchanged. Null-guards on `lookupApte`/`lookupApte2`; `showMeterInfo`/`runCrossCheck` now `async` + await the loader (cross-check bails with a message on load failure).
- [x] Verified: `apteFetchedAtLoad: 0` (not pulled on initial load), JSON serves 200/90+106 entries, cross-check renders 106 (APTE2) + 90 (APTE1) rows with correct match stats, fetched once across 3 triggers, zero console errors, `node --check` clean.
- [x] Only app.js consumes this data (Meter Info popup + cross-check modal) — render.html/headless path unaffected.

### 2026-06-13 — SRI hashes for mp4-muxer CDN

- [x] Pinned the floating `mp4-muxer` CDN URL to `@5.2.2` + added `integrity="sha384-wr0AQH9…"` + `crossorigin="anonymous"` in all 4 references: `index.html`, `student.html`, `tools/templates/student.html` (source of truth — else regen wipes it), `render.html`.
- [x] Pinning is mandatory with SRI: a floating URL + integrity would break MP4 export the moment jsdelivr ships a new version. Pinned hash == floating hash today, so byte-identical / behavior-preserving.
- [x] Verified: index.html loads the script under SRI — `Mp4Muxer.Muxer` defined, zero integrity/CORS console errors.

### 2026-06-13 — Timing math extraction (ADR-0001)

- [x] New `src/core/timing.js`: pure module — `detectOnsetsFromPcm` (PCM in, no AudioBuffer), `snapToNearest`, `snapConfidence`, `moraUnits`, `distributePada`, `padaUnitDuration`, `scaleTiming`. Numeric defaults (`ONSET_DEFAULTS`) mirror `tools/alignment_params.json`.
- [x] app.js wrappers (strangler pattern, globals stay put): `detectOnsets` → 1-line PCM adapter; `_snapToNearest`/`_snapConfidence` deleted, now imported; `calcAutoTiming` inner loop → `distributePada`; laghu readout → `padaUnitDuration`; `corpusScaleTiming` map → `scaleTiming`.
- [x] Verified: library surface — all pure fns match analytic goldens; `detectOnsetsFromPcm` **byte-identical** to old inline algo (deep-equal on synthetic PCM). App surface — Run BG 2.47 renders 2 wave SVGs (path + "kar"… labels), zero console errors. `node --check` clean.
- [x] **Correction:** the old "blocks ADR-0002 headless color config" note was stale — `render.html` reads pre-baked `verseJson.timing`, never calls these. Real payoff: completes ADR-0001's last logic block + makes timing math unit-testable + enables future Node-side auto-timing.

### 2026-06-13 — Content/copy review (continued)

**Content/copy — medium items**
- [x] Student help modal i18n: 8 keys added to strings.js (RU+EN), `data-i18n-html` handler added to `applyI18n()`, `data-i18n`/`data-i18n-html` attrs wired in student.html modal; verified both languages in browser
- [x] Mixed transliteration: "тоṭака" (mixed Cyrillic+IAST) → "тотака" (pure Cyrillic) in catalogue.html tooltip
- [x] Cache-bust: `strings.js?v=1.4.2` query string added to student.html script tag

### 2026-06-13 — Multi-review session

**Code review**
- [x] Dead DEV_* transliteration tables removed from app.js (429 lines; orphaned after `devToIast` delegated to core/translit.js)
- [x] Comma-operator bug fixed in `compose.js` (`Math.round(mc.font = …)` → separate assignments)
- [x] Defensive `Math.max(0, …)` guard added to `svg.js` for empty syllable arrays
- [x] ADR-0001 strangler-fig delegation verified: `node --check` passes, wave diagram renders in browser, zero console errors

**UX review + 8 fixes**
- [x] Mobile responsive breakpoint added to `src/style.css` (`.app` grid collapses at 640px)
- [x] `outline: none` removed from textarea/input; `focus-visible` ring added for keyboard accessibility
- [x] Mode switch button: `title` tooltip added explaining padas vs. syllable timing
- [x] Empty state replaced with 3-step onboarding guide (i18n keys added to strings.js for RU + EN)
- [x] Sidebar reordered: audio + timing block now appears BEFORE export block (`tools/_fix_sidebar_order.py`)
- [x] `catalogue.html`: back-to-editor link added; meter filter label improved; "Все размеры" → "Все метры"
- [x] `progress.html`: back-to-editor link added; sync button improved with descriptive states; lang toggle added
- [x] `progress.html`: JS state strings updated (☁ username, ☁ Открываю Google…, ☁ Синхронизация…)

**Security review + 3 fixes**
- [x] Drive `_gdRenderList()` XSS: replaced template-literal innerHTML with `createElement` + `addEventListener` (no user string in any HTML attribute)
- [x] `showMsg()` HTML escaping completed: added `>` and `"` to replace chain
- [x] `student.html` verse ID whitelist: `!/^[a-zA-Z0-9_-]+$/.test(id)` guard before `fetch()`

**Data/schema review + 3 fixes**
- [x] `verse.schema.json`: `duration_s` type changed to `["number","null"]` with description
- [x] All 3 sample verses: `duration_s: 15.5` → `null` (was template sentinel, misleads eval tools)
- [x] `validate_library.py`: cross-file `permission_ref` uniqueness check added (errors on duplicate, non-zero exit)
- [x] `CLAUDE.md` DATA struct table expanded: `arrow`, `vipulaType`, `TAP.confidence` all documented with semantics

**Architecture review + 5 fixes**
- [x] Context menu listener leak: `document.addEventListener('click', closeCtx)` moved from `render()` to module-init (was accumulating per-render)
- [x] `runPipeline()` error boundary: core execution wrapped in `try/catch` with `showMsg` recovery
- [x] `_getWaveColors()` helper extracted from `buildWaveSVG()`: centralises CSS-var reads; headless renderers can override without touching call sites
- [x] `strings.js` loaded in `progress.html` (was only in `index.html` + `catalogue.html`)
- [x] `progress.html`: `data-i18n="progressHeading"` wired to `<h1>`; `progressHeading` key added to both RU + EN string blocks; lang toggle button added

### Earlier sessions
- [x] v1.4.2 first release prep: version metadata, changelog, cache key, cloud/progress hardening
- [x] v1.4.1 hardening: Firebase local-to-cloud migration, progress mobile polish, student telemetry sync
- [x] v1.4.0: version unification + SW fix + design consistency + docs
- [x] ADR-0001 strangler-fig: app.js → ES module; core extraction (translit, layout, svg, compose, karaoke-frame); window.X block for ~55 handlers
- [x] ADR-0002 headless renderer: render.html + tools/render_chapter.js + Puppeteer + mp4-muxer
- [x] ADR-0003 auto-alignment: align_chapter.py + alignment_params.json + eval_alignment.py
- [x] Legal: Usha Sanka license agreement draft (SK-LIC-2026-001)
- [x] Schema: audio rights fields, translation provenance, 3 verses backfilled
