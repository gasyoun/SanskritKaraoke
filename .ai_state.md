# Project Objective: Sanskrit Karaoke v1.4.2 first release

## Next Steps (Queue)

- [ ] Karaoke product roadmap (see `docs/KARAOKE_PRODUCT_ROADMAP.md`, decided 2026-06-12):
  - [ ] Phase 0: written permission from Уша Санка + rights/provenance fields in verse schema + FAIR audio hosting.
    - [x] Agreement draft generated: `docs/legal/Usha_Sanka_License_Agreement_DRAFT.docx` (SK-LIC-2026-001; regenerate via `node docs/legal/make_agreement.js`). Open blanks: addresses, governing law (cl. 7.1), consideration alternative (cl. 3.1), Annex A dates/durations. Recording IDs USK-2026-00N → `audio.permission_ref`.
    - [x] Schema: `audio` now requires `rights_holder`/`license`/`permission_ref` (+ optional `sha256`, `canonical_url`); `translation.provenance` added. 3 verses backfilled (USK-2026-001..003, provenance=unknown); `validate_library.py` lints TODO drive ids / missing checksums / unknown provenance (warnings, exit 0).
    - [ ] Resolve translation provenance: EN translations of bhg_2_47/2_49 look like Prabhupāda (BBT copyright) — replace with own translations or confirm rights; set provenance to human/human-reviewed.
  - [ ] Phase 1: auto-alignment CLI (`tools/align_chapter.py`) — build per `docs/adr/0003`; FIRST build `tools/eval_alignment.py` gold harness (≥90% syllables within ±50ms).
  - [ ] Phase 2: headless 9:16 batch renderer @30fps + feed template + .srt/.vtt export — per `docs/adr/0002` (Puppeteer + render.html, NOT node-canvas first, NOT Remotion).
  - [ ] Core extraction (enables Phase 2): per `docs/adr/0001` — src/core/ ES modules, no bundler; ship `compose.js` + `karaoke-frame.js` first; move APTE_METERS blobs to lazy JSON.
  - [ ] Quick win: raise in-app karaoke export FPS 10→30 (app.js:1873, one constant).
  - [ ] Phase 3: post-kit generator with UTM-tagged course CTAs (free-video → paid-course funnel).
- [x] Add real Firebase credentials to `src/scripts/firebase-config.js`.
- [ ] Run a real cloud-sync acceptance test: student SRS rating -> Firestore -> second browser/device -> `progress.html`.
- [ ] Verify Google login/logout behavior against the deployed origin.
- [ ] Resume Firebase acceptance from Google password/2FA/consent; see `docs/FIREBASE_ACCEPTANCE.md`.
- [ ] Add real Google Drive audio file IDs to the 3 example verses.
- [ ] Phase 1: Build `verse_agent_sdk.py` (Claude Agent SDK rebuild).
- [ ] Phase 1: Write `agents/POSTMORTEM.md` (raw SDK vs Claude Agent SDK).

## Current Work-In-Progress (WIP)

- [x] Firebase migration pass: local SRS/telemetry/progress metadata now merge into Firestore on login.
- [x] Progress dashboard mobile polish: summary stats, stacked mobile rows, duplicate-render guard.
- [x] Student telemetry sync hook and generated `student.html` refresh.
- [x] v1.4.0 version unification (one track, semver).
- [x] SW cache staleness fix (activate handler + cache pruning).
- [x] Design system consistency (catalogue.html font family).
- [x] Stale docs cleanup (AGENTS.md, MY_ROADMAP.md, WAVE_INSTRUCTION).

## Dev Notes & Hypotheses

- 2026-06-02: `cloud_sync.js` now treats Firebase placeholders as an explicit disabled state, lazy-loads Firebase modules only after real config is present, and still exposes local fallback behavior.
- 2026-06-03: Firebase web config for project `sanskritkaraoke` is committed; production cloud-sync acceptance is ready to run against GitHub Pages.
- 2026-06-03: Live Firebase acceptance reached the Firebase popup handler but the in-app browser showed "The requested action is invalid."; cloud login now uses redirect auth instead of popup auth.
- 2026-06-03: Firebase config and cloud-sync script tags now carry version queries so GitHub Pages clients do not reuse stale auth code during acceptance.
- 2026-06-03: Live redirect acceptance is blocked by Firebase Auth `auth/configuration-not-found`; enable Firebase Authentication and Google provider, then verify `gasyoun.github.io` is authorized.
- 2026-06-03: Firestore rules are committed for `users/{uid}/data/{srs_v1,telemetry,progress_meta}` with `{ payload, updated_at }`, but they still need deployment in Firebase.
- 2026-06-03: User reported Firebase Console setup is done: Google provider, support email, authorized domain, Firestore, and rules. Live auth now reaches Google Accounts for `gasyoun@gmail.com`; acceptance is paused at password/2FA/consent on `accounts.google.com/v3/signin/challenge/pwd`.
- 2026-06-02: Live GitHub Pages acceptance found and fixed a generated `student.html` visible version badge still showing the legacy `v1.267` track; service-worker HTML handling now uses network-first `cache: reload` so query-string student URLs do not stay stale after HTML fixes.
- 2026-06-02: Telegram BackButton calls are guarded by SDK version support checks to avoid unsupported-version warnings in regular browser sessions.
- 2026-06-02: Catalogue and progress now show visible `v1.4.2` release badges, matching main and student surfaces.
- 2026-06-02: Cloud migration is implemented but not production-accepted until real Firebase config and a second-device login test are run.
- 2026-06-02: `progress_meta` bridges legacy `shloka_last_played` / `shloka_streak` localStorage keys into cloud sync without changing the existing student UI contract.
- 2026-05-14: Version numbering was split into two tracks: incremental (`v1` -> `v267` -> `v1.272`) from manual work and semver (`1.0.0` -> `1.3.1`) from Gemini. Unified to `v1.4.0`.
- 2026-05-14: `make_student.py` is planned for use but was not actually run by the teacher yet. Regex-based HTML surgery is fragile; consider adding output validation.
- 2026-05-14: Auto-alignment plan (`GEMINI_ALIGNMENT_PLAN.md`) is assigned to Gemini Flash, not Claude.

## Completed (Recent only)

- [x] v1.4.2 first release prep: version metadata, changelog, cache key, cloud/progress hardening.
- [x] v1.4.1 hardening pass: Firebase local-to-cloud migration, progress dashboard mobile polish, student telemetry sync.
- [x] Full codebase review (all files, 12 layers).
- [x] v1.4.0 release: version unification + SW fix + design consistency + docs.
