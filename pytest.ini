[pytest]
# Collect only tests/: tools/test_meter_detector.py is a standalone script
# that calls sys.exit() at import time and aborts bare-pytest collection.
testpaths = tests
markers =
    integration: tests that call external services or may incur API cost
